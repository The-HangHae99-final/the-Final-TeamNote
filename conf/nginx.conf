server {
     listen 80;
     listen [::]:80;

     http://localhost:3000; // 등록한 도메인으로 변경

     location /.well-known/acme-challenge/ {
             allow all;
             root /var/www/certbot;
     } 
}